import { CachedBounds, SizeFunction } from './types';

export declare function createCachedBounds<Props extends object>({ itemCount, itemProps, itemSize }: {
    itemCount: number;
    itemProps: Props;
    itemSize: number | SizeFunction<Props>;
}): CachedBounds;
//# sourceMappingURL=createCachedBounds.d.ts.map